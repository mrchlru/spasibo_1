<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Spasibo" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/app_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Splash.Spasibo" parent="Theme.SplashScreen">
        <item name="android:windowBackground">@color/logo_background</item>
        <item name="android:statusBarColor">@color/logo_background</item>
        <item name="android:navigationBarColor">@color/logo_background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="windowSplashScreenBackground">@color/logo_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/logo_background</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Theme.Spasibo</item>
    </style>
</resources>
