# Исправление проблемы с автоматическими миграциями БД

## Проблема

Автоматические миграции БД не срабатывали при деплое по следующим причинам:

### 1. **Ошибки миграций игнорировались**
В `app.py` (строки 39-42) ошибки миграций логировались как `warning` и не прерывали запуск приложения:
```python
except Exception as e:
    logger.warning(f"⚠️ Миграция {migration_file.name}: {e}")
```
Это означало, что если миграция падала с ошибкой, приложение все равно запускалось, и проблема оставалась незамеченной.

### 2. **Отсутствие системы отслеживания примененных миграций**
- Миграции применялись каждый раз при старте приложения заново
- Не было проверки, была ли миграция уже применена
- Это могло приводить к ошибкам при повторном применении миграций

### 3. **Недостаточное логирование**
- Не было подробных логов о процессе миграций
- Сложно было понять, какие миграции были применены, а какие нет
- Не было информации о путях к файлам миграций при деплое

## Решение

### 1. **Создана таблица отслеживания миграций**
Создана миграция `000_create_migrations_table.sql`, которая создает таблицу `schema_migrations` для отслеживания примененных миграций.

### 2. **Улучшена логика применения миграций в `app.py`**
- ✅ Проверка существования папки migrations с подробным логированием путей
- ✅ Создание таблицы отслеживания миграций перед применением
- ✅ Проверка уже примененных миграций перед их выполнением
- ✅ Правильная обработка ошибок - при ошибке миграции приложение не запускается
- ✅ Подробное логирование каждого шага процесса миграций
- ✅ Запись факта применения миграции в таблицу отслеживания

### 3. **Обновлен скрипт `run_migrations.py`**
Скрипт теперь использует ту же логику отслеживания миграций, что и автоматическое применение при старте.

## Что нужно проверить при деплое

1. **Логи запуска приложения** - должны содержать:
   - `✅ Папка migrations найдена: ...`
   - `✅ Таблица schema_migrations создана/проверена`
   - `📋 Уже применено миграций: X`
   - `📄 Применение миграции: ...`
   - `✅ Миграция ... применена успешно`

2. **Если миграция не применяется**, проверьте логи на наличие:
   - `❌ КРИТИЧЕСКАЯ ОШИБКА при применении миграции ...`
   - Полный traceback ошибки

3. **Проверка в БД**:
   ```sql
   SELECT * FROM schema_migrations ORDER BY applied_at;
   ```
   Должны быть записи для всех примененных миграций.

## Дополнительная информация для диагностики

Если миграции все еще не применяются, проверьте:

1. **Путь к папке migrations**:
   - В логах должно быть: `✅ Папка migrations найдена: ...`
   - Если видите `❌ Папка migrations не найдена`, проверьте структуру проекта при деплое

2. **Подключение к БД**:
   - Убедитесь, что переменная окружения `DATABASE_URL` правильно настроена
   - Проверьте, что приложение может подключиться к БД при старте

3. **Права доступа**:
   - Убедитесь, что приложение имеет права на создание таблиц и выполнение SQL

4. **Транзакции**:
   - Каждая миграция выполняется в отдельной транзакции
   - Если миграция падает, она откатывается, но приложение не запустится

## Важные изменения в поведении

⚠️ **ВНИМАНИЕ**: Теперь при ошибке миграции приложение **НЕ ЗАПУСТИТСЯ**. Это сделано намеренно, чтобы проблемы с миграциями были заметны сразу.

Если нужно временно пропустить проблемную миграцию:
1. Исправьте миграцию
2. Или временно переименуйте файл миграции (добавьте префикс, например `_skip_`)
3. Или вручную добавьте запись в `schema_migrations` для пропуска миграции
